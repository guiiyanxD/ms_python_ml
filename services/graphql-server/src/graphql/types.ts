export type ModelAType = {
  id: string;
  parameterA: string;
  parameterB: number;
};

export type ModelBType = {
  id: string;
  parameterC: boolean;
  parameterD: string[];
};

export type ModelCType = {
  id: string;
  parameterE: Date;
  parameterF: string;
};

export type ModelDImageType = {
  id: string;
  imageUrl: string;
  parameterG: string;
};